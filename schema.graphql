type PoolCreationEntity @entity {
  id: ID!
  timestamp: BigInt! # address
  poolAddress: Bytes! # address
}

type MetaversepadPool @entity {
  id: ID!
  address: Bytes! # address
  balance: BigInt!
}